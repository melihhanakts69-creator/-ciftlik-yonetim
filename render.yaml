services:
  - type: web
    name: ciftlik-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      # GEMINI_API_KEY ve MONGODB_URI Render panosunda Environment olarak tanimlayin — repoya yazmayin
